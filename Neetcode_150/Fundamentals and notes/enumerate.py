#June 9 2025
# notes on enemurate

# Enumerate() is a built in function which helps to iterate both index and the element
# By default, the index is the first element of the tuple, and the value is the second.

# example use
colors = ['red','green','yellow','white']

for index,color in enumerate(colors):
    print(index,color)
